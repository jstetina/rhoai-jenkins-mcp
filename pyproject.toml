[project]
name = "rhoai-jenkins-mcp"
version = "0.1.0"
description = "MCP server for RHOAI Jenkins CI/CD operations"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jenkinsapi>=0.3.14",
    "mcp>=1.25.0",
    "python-jenkins",
    "uvicorn>=0.34.2",
    "httpx>=0.27.0",
]
