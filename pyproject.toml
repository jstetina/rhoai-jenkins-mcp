[project]
name = "jenkins-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jenkinsapi>=0.3.14",
    "mcp>=1.8.1",
    "python-jenkins",
    "uvicorn>=0.34.2",
    "httpx>=0.27.0",
]
